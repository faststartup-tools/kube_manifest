apiVersion: v1
kind: Service
metadata:
  name: nextjs-boilerplate-service
spec:
  selector:
    app: nextjs-boilerplate

  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  

